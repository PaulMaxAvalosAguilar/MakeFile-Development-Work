#Library name
LIBNAME     := $(MODULEB)

#write -I $(INCDIR)
INCDEP      := 


PACKAGINGNAME		:= $(LIBDIR)/lib$(LIBNAME).$(SLEXT)
PACKAGINGINSTRUCTIONS	:= $(ARCH)
COMPILATIONINSTURCTIONS	:= $(CC) $(CFLAGS) $(MACROS) -c $(INCDEP)